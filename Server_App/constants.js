const serverEvents = {
    getHistory: 'getHistory',
    pullMessage: 'pullMessage',
    signoff: 'signoff',
}

const userTypes = {
    anonymous: 'Anonymous'
}

export {
    serverEvents,
    userTypes
}